Let $f(x) = 7 + 3 x + 5 x^2 + 11 x^3 + 7 x^4$.

\medskip \noindent \textbf{(a)} Compute $f(0)$, $f'(0)$, $f''(0)$, $f^{(3)}(0)$, $f^{(4)}(0)$.

\vspace{60mm}

\medskip \noindent \textbf{(b)} Given the coefficients in the original function, there is a pattern to the derivative values you computed.  Describe the pattern.
